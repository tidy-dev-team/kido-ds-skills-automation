---
description: Generate a Figma component set from any client input (Figma frame, screenshot, site URL, repo, description). Uses Kido structure + client brand values. Use when client has no existing UI library. Arguments: component name + any client input (URL, image, description).
---

Read `skills/ds-generate.md` and follow its instructions.

ARGUMENTS: $ARGUMENTS
