---
description: Build canvas documentation for a polished component. Produces three Figma frames on a 📄 Documentation page in the target file — Component Breakdown (variants grid, max/min size, icon positions), Mode (one cell per theme.* in tokens.json), and Usage Guidelines (When-to-use / When-not-to-use / General / Behavior / Content / Look & Feel). Reads prose from specs/{component}.spec.notes.md and Do/Don't visual examples from specs/{component}.dodont.json. Workflow B only for v1 — requires working/{project}/tokens.json. Arguments: component name + polished Figma component-set URL.
---

Read `skills/ds-doc.md` and follow its instructions.

ARGUMENTS: $ARGUMENTS
