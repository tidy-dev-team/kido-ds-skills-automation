---
description: Generate component documentation as markdown in the chat — variant descriptions and a Usage Guidelines section (When-to-use / When-not-to-use / General / Behavior / Content / Accessibility) — from a polished Figma component-set URL. Reads the component via the official Figma MCP only; never edits the canvas. Synthesises Usage Guidelines from `specs/{component}.guidelines.json` (recommended=true entries) when available, otherwise searches the web for industry best practices. Arguments: polished Figma component-set URL.
---

Read `skills/ds-doc.md` and follow its instructions.

ARGUMENTS: $ARGUMENTS
