---
description: Extract design tokens from one or more Figma foundation files → two sibling files in working/{project}/ — tokens.json (full W3C DTCG token tree, source of truth) and DESIGN.md (google-labs-code/design.md-compliant front matter + prose rationale). Supports split foundations (e.g. colors file + tokens file). Run before /ds-build. Arguments: one or more Figma URLs + optional project slug.
---

Read `skills/ds-extract-design.md` and follow its instructions.

ARGUMENTS: $ARGUMENTS
