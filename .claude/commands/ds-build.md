---
description: Build a Figma component set matching a client's existing UI library (Mantine, Chakra, shadcn, custom), styled with tokens from working/{project}/tokens.json (the DTCG sibling of DESIGN.md, both produced by /ds-extract-design). Runs validator at end. Use when client already ships a UI library. Arguments: component name + optional library reference or Figma URL.
---

Read `skills/ds-build.md` and follow its instructions.

ARGUMENTS: $ARGUMENTS
