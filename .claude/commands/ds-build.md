---
description: Build a Figma component set matching a client's existing UI library (Mantine, Chakra, shadcn, custom), styled with DESIGN.md tokens. Runs validator at end. Use when client already ships a UI library. Arguments: component name + optional library reference or Figma URL.
---

Read `skills/ds-build.md` and follow its instructions.

ARGUMENTS: $ARGUMENTS
