---
description: Generate a Storybook story file (CSF3) for a production-ready component. Run after ds-push. Arguments: component name + source (GitHub repo URL or local project path) + optional separate local Storybook folder (Hybrid mode) + optional Storybook URL (defaults to http://localhost:6006) + optional Figma component URL.
---

Read `skills/ds-storybook.md` and follow its instructions.

ARGUMENTS: $ARGUMENTS
