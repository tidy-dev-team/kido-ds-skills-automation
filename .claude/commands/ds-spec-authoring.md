---
description: Author or update a Kido DS component spec (specs/*.spec.json + notes). DS team only — not a per-project task. Arguments: component name + optional Kido DS Figma URL.
---

Read `skills/ds-spec-authoring.md` and follow its instructions.

ARGUMENTS: $ARGUMENTS
